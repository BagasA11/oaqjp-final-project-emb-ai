# Repository for final project
#### Emotion Detection Using Flask
#### Guide
1. create venv and install required dependencies using `pip`
2. then run `server.py`
3. open web-browser and browse `localhost:5000?input=`
4. you can fill `input` argument using any text  